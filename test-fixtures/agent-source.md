# Agent 工具调用实验

Agent 不是只会回答的模型。它需要根据目标制定计划，调用工具，读取 Observation，并在停止条件满足时结束。

## 退款场景

客服 Agent 应先查询订单状态，再调用退款工具。工具返回结果后必须校验退款单是否创建成功；证据不足时不得声称退款已完成。

## ReAct 循环

Thought 用于形成下一步判断，Action 表示工具调用，Observation 是工具返回的事实。可靠系统还需要超时、重试、预算与人工接管。
